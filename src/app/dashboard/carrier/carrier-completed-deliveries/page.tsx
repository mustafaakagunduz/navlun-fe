import CompletedDeliveriesContent from './CompletedDeliveriesContent';

export default function CompletedDeliveriesPage() {
    return (
        <div className="p-8">
            <CompletedDeliveriesContent />
        </div>
    );
}
